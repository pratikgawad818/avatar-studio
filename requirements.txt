# Avatar Studio Professional - Complete Requirements
# High-quality AI video generation with undetectable output

# ═══════════════════════════════════════════════════════════════════════════
# CORE DEPENDENCIES
# ═══════════════════════════════════════════════════════════════════════════

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Computer Vision & Image Processing
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0

# Deep Learning & AI
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scipy>=1.11.0

# ═══════════════════════════════════════════════════════════════════════════
# VOICE & AUDIO PROCESSING
# ═══════════════════════════════════════════════════════════════════════════

# TTS & Voice Cloning
edge-tts>=6.1.9
# F5-TTS (install separately: pip install git+https://github.com/SWivid/F5-TTS.git)

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0
pydub>=0.25.1
noisereduce>=3.0.0

# ═══════════════════════════════════════════════════════════════════════════
# VIDEO ENHANCEMENT & SUPER-RESOLUTION
# ═══════════════════════════════════════════════════════════════════════════

# Face Enhancement
gfpgan>=1.3.8
# basicsr>=1.4.2 (installed with gfpgan)

# Super-Resolution
realesrgan>=0.3.0
# Install with: pip install realesrgan

# ═══════════════════════════════════════════════════════════════════════════
# LIP-SYNC & FACE DETECTION
# ═══════════════════════════════════════════════════════════════════════════

# Wav2Lip dependencies (already in project)
# Install from models/Wav2Lip/requirements.txt

# Face Detection & Landmarks
dlib>=19.24.0  # Requires cmake
face-recognition>=1.3.0
mediapipe>=0.10.0

# ═══════════════════════════════════════════════════════════════════════════
# BACKGROUND REMOVAL & SEGMENTATION
# ═══════════════════════════════════════════════════════════════════════════

# Background Removal
rembg[gpu]>=2.0.50  # Use 'rembg' for CPU-only
# Alternative: pip install rembg

# Depth Estimation
# MiDaS (installed via torch.hub automatically)

# ═══════════════════════════════════════════════════════════════════════════
# SUBTITLES & TRANSCRIPTION
# ═══════════════════════════════════════════════════════════════════════════

# Whisper AI
openai-whisper>=20230314
# Or faster-whisper for better performance:
# faster-whisper>=0.9.0

# Subtitle Handling
pysrt>=1.1.2
srt>=3.5.3

# ═══════════════════════════════════════════════════════════════════════════
# UTILITIES & HELPERS
# ═══════════════════════════════════════════════════════════════════════════

# File & Path Handling
pathlib>=1.0.1  # Built-in, but listed for clarity
python-dotenv>=1.0.0

# Progress & Logging
tqdm>=4.66.0
colorama>=0.4.6

# Data Processing
pandas>=2.0.0  # Optional, for analytics

# ═══════════════════════════════════════════════════════════════════════════
# SYSTEM REQUIREMENTS
# ═══════════════════════════════════════════════════════════════════════════

# FFmpeg is REQUIRED (install via system package manager)
# macOS: brew install ffmpeg
# Ubuntu: sudo apt-get install ffmpeg
# Windows: Download from ffmpeg.org

# FFprobe (included with FFmpeg)

# Additional system dependencies:
# - cmake (for dlib): brew install cmake
# - pkg-config: brew install pkg-config

# ═══════════════════════════════════════════════════════════════════════════
# OPTIONAL ENHANCEMENTS
# ═══════════════════════════════════════════════════════════════════════════

# GPU Acceleration (CUDA for NVIDIA GPUs)
# Install PyTorch with CUDA support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# MPS (Apple Silicon) - PyTorch 2.0+ includes MPS support by default

# Code Quality
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.5.0

# ═══════════════════════════════════════════════════════════════════════════
# INSTALLATION NOTES
# ═══════════════════════════════════════════════════════════════════════════

# 1. Create virtual environment:
#    python3 -m venv venv
#    source venv/bin/activate  # On Windows: venv\Scripts\activate

# 2. Install basic requirements:
#    pip install -r requirements_pro.txt

# 3. Install F5-TTS (optional, for voice cloning):
#    pip install git+https://github.com/SWivid/F5-TTS.git

# 4. Download Wav2Lip checkpoint:
#    Place wav2lip_gan.pth in models/Wav2Lip/checkpoints/

# 5. Verify FFmpeg installation:
#    ffmpeg -version
#    ffprobe -version

# 6. Test installation:
#    python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA: {torch.cuda.is_available()}'); print(f'MPS: {torch.backends.mps.is_available()}')"

# ═══════════════════════════════════════════════════════════════════════════
# TROUBLESHOOTING
# ═══════════════════════════════════════════════════════════════════════════

# If dlib installation fails:
#   - Install cmake first: brew install cmake
#   - Or use pre-built wheel: pip install dlib-binary

# If rembg installation fails:
#   - Try: pip install rembg --no-deps
#   - Then: pip install onnxruntime

# For Apple Silicon (M1/M2/M3):
#   - Use Python 3.10 or 3.11 (best compatibility)
#   - Some packages may need conda: conda install -c conda-forge opencv

# Memory requirements:
#   - Minimum: 8GB RAM
#   - Recommended: 16GB RAM
#   - With GPU: 6GB+ VRAM for best quality
